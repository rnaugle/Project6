DataFrag(
	.data
Factorial_vtable:
	.word Factorial.main

)
DataFrag(
	.data
Fac_vtable:
	.word Fac.ComputeFac

)
ProcFrag(
MipsFrame(
main:
Formals(InReg(t33)
	InReg(t34)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(8)
)
  MOVE("	move	`d0,	`s0" defs(t41 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t42 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t43 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t44 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t45 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t46 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t47 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t48 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t49 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t50 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t33 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t34 ) uses(t5 ))
  LABEL("L6")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t0 ))
  OPER("	la	`d0,	Fac_vtable" defs(t51 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t51 ))
  OPER("	jal	_new" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t35 ) uses(t2 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t35 t0 ) jumps(_BADPTR L0 ))
  LABEL("L0")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t35 ))
  OPER("	addi	`d0,	`s0,	10" defs(t52 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t52 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t54 ) uses(t35 ))
  OPER("	lw	`d0,	0(`s0)" defs(t53 ) uses(t54 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t53 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t40 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t40 ))
  OPER("	jal	_printint" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t0 ))
  OPER("	b	`j0" jumps(L5 ))
  LABEL("L5")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t50 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t49 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t48 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t47 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t46 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t45 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t44 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t43 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t42 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t41 ))
)
ProcFrag(
MipsFrame(
Fac.ComputeFac:
Formals(InReg(t36)
	InReg(t37)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(8)
)
  MOVE("	move	`d0,	`s0" defs(t57 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t58 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t59 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t60 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t61 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t62 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t63 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t64 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t65 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t66 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t36 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t37 ) uses(t5 ))
  LABEL("L8")
  MOVE("	move	`d0,	`s0" defs(t38 ) uses(t0 ))
  OPER("	addi	`d0,	`s0,	1" defs(t67 ) uses(t0 ))
  OPER("	blt	`s0,	`s1,	`j0" uses(t37 t67 ) jumps(L2 L3 ))
  LABEL("L3")
  MOVE("	move	`d0,	`s0" defs(t56 ) uses(t37 ))
  MOVE("	move	`d0,	`s0" defs(t39 ) uses(t36 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t39 t0 ) jumps(_BADPTR L1 ))
  LABEL("L1")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t39 ))
  OPER("	addi	`d0,	`s0,	-1" defs(t68 ) uses(t37 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t68 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t70 ) uses(t39 ))
  OPER("	lw	`d0,	0(`s0)" defs(t69 ) uses(t70 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t69 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t55 ) uses(t2 ))
  OPER("	mulo	`d0,	`s0,	`s1" defs(t71 ) uses(t56 t55 ))
  MOVE("	move	`d0,	`s0" defs(t38 ) uses(t71 ))
  LABEL("L4")
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t38 ))
  OPER("	b	`j0" jumps(L7 ))
  LABEL("L2")
  OPER("	addi	`d0,	`s0,	1" defs(t72 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t38 ) uses(t72 ))
  OPER("	b	`j0" jumps(L4 ))
  LABEL("L7")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t66 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t65 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t64 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t63 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t62 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t61 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t60 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t59 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t58 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t57 ))
)
