DataFrag(
	.data
Main_vtable:
	.word Main.main

)
DataFrag(
	.data
Thread1_vtable:
	.word Thread1.run
	.word Thread1.setData

)
DataFrag(
	.data
Thread2_vtable:
	.word Thread2.run
	.word Thread2.setData

)
DataFrag(
	.data
Data_vtable:
	.word Data.set_field
	.word Data.increment_one
	.word Data.increment_ten
	.word Data.write
	.word Data.field_print

)
ProcFrag(
MipsFrame(
main:
Formals(InReg(t33)
	InReg(t34)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(8)
)
  MOVE("	move	`d0,	`s0" defs(t90 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t91 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t92 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t93 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t94 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t95 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t96 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t97 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t98 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t99 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t33 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t34 ) uses(t5 ))
  LABEL("L33")
  MOVE("	move	`d0,	`s0" defs(t35 ) uses(t0 ))
  OPER("	addi	`d0,	`s0,	2" defs(t100 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t100 ))
  OPER("	la	`d0,	Thread1_vtable" defs(t101 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t101 ))
  OPER("	jal	_new" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t36 ) uses(t2 ))
  OPER("	addi	`d0,	`s0,	2" defs(t102 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t102 ))
  OPER("	la	`d0,	Thread2_vtable" defs(t103 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t103 ))
  OPER("	jal	_new" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t37 ) uses(t2 ))
  OPER("	addi	`d0,	`s0,	1" defs(t104 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t104 ))
  OPER("	la	`d0,	Data_vtable" defs(t105 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t105 ))
  OPER("	jal	_new" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t38 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t39 ) uses(t0 ))
  MOVE("	move	`d0,	`s0" defs(t40 ) uses(t38 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t40 t0 ) jumps(_BADPTR L0 ))
  LABEL("L0")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t40 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t0 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t107 ) uses(t40 ))
  OPER("	lw	`d0,	0(`s0)" defs(t106 ) uses(t107 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t106 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t87 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t35 ) uses(t87 ))
  MOVE("	move	`d0,	`s0" defs(t41 ) uses(t36 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t41 t0 ) jumps(_BADPTR L1 ))
  LABEL("L1")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t41 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t38 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t109 ) uses(t41 ))
  OPER("	lw	`d0,	4(`s0)" defs(t108 ) uses(t109 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t108 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t88 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t39 ) uses(t88 ))
  MOVE("	move	`d0,	`s0" defs(t42 ) uses(t37 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t42 t0 ) jumps(_BADPTR L2 ))
  LABEL("L2")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t42 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t38 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t111 ) uses(t42 ))
  OPER("	lw	`d0,	4(`s0)" defs(t110 ) uses(t111 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t110 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t89 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t39 ) uses(t89 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t37 ))
  OPER("	jal	_threadCreate" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t36 ))
  OPER("	jal	_threadCreate" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t0 ))
  OPER("	b	`j0" jumps(L32 ))
  LABEL("L32")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t99 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t98 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t97 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t96 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t95 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t94 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t93 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t92 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t91 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t90 ))
)
ProcFrag(
MipsFrame(
Thread1.run:
Formals(InReg(t43)
	)
Actuals(InReg(t4)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(4)
)
  MOVE("	move	`d0,	`s0" defs(t116 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t117 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t118 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t119 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t120 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t121 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t122 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t123 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t124 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t125 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t43 ) uses(t4 ))
  LABEL("L35")
  MOVE("	move	`d0,	`s0" defs(t44 ) uses(t43 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t44 t0 ) jumps(_BADPTR L3 ))
  LABEL("L3")
  OPER("	addi	`d0,	`s0,	4" defs(t126 ) uses(t44 ))
  MOVE("	move	`d0,	`s0" defs(t113 ) uses(t126 ))
  MOVE("	move	`d0,	`s0" defs(t45 ) uses(t43 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t45 t0 ) jumps(_BADPTR L4 ))
  LABEL("L4")
  OPER("	lw	`d0,	0(`s0)" defs(t127 ) uses(t45 ))
  MOVE("	move	`d0,	`s0" defs(t46 ) uses(t127 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t46 t0 ) jumps(_BADPTR L5 ))
  LABEL("L5")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t46 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t129 ) uses(t46 ))
  OPER("	lw	`d0,	4(`s0)" defs(t128 ) uses(t129 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t128 t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t112 ) uses(t2 ))
  OPER("	sw	`s0,	(`s1)" uses(t112 t113 ))
  MOVE("	move	`d0,	`s0" defs(t47 ) uses(t43 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t47 t0 ) jumps(_BADPTR L6 ))
  LABEL("L6")
  OPER("	addi	`d0,	`s0,	4" defs(t130 ) uses(t47 ))
  MOVE("	move	`d0,	`s0" defs(t115 ) uses(t130 ))
  MOVE("	move	`d0,	`s0" defs(t48 ) uses(t43 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t48 t0 ) jumps(_BADPTR L7 ))
  LABEL("L7")
  OPER("	lw	`d0,	0(`s0)" defs(t131 ) uses(t48 ))
  MOVE("	move	`d0,	`s0" defs(t49 ) uses(t131 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t49 t0 ) jumps(_BADPTR L8 ))
  LABEL("L8")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t49 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t133 ) uses(t49 ))
  OPER("	lw	`d0,	16(`s0)" defs(t132 ) uses(t133 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t132 t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t114 ) uses(t2 ))
  OPER("	sw	`s0,	(`s1)" uses(t114 t115 ))
  OPER("	b	`j0" jumps(L34 ))
  LABEL("L34")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t125 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t124 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t123 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t122 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t121 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t120 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t119 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t118 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t117 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t116 ))
)
ProcFrag(
MipsFrame(
Thread1.setData:
Formals(InReg(t50)
	InReg(t51)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(0)
)
  MOVE("	move	`d0,	`s0" defs(t134 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t135 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t136 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t137 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t138 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t139 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t140 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t141 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t142 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t143 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t50 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t51 ) uses(t5 ))
  LABEL("L37")
  MOVE("	move	`d0,	`s0" defs(t52 ) uses(t50 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t52 t0 ) jumps(_BADPTR L9 ))
  LABEL("L9")
  OPER("	sw	`s0,	0(`s1)" uses(t51 t52 ))
  MOVE("	move	`d0,	`s0" defs(t53 ) uses(t50 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t53 t0 ) jumps(_BADPTR L10 ))
  LABEL("L10")
  OPER("	lw	`d0,	0(`s0)" defs(t144 ) uses(t53 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t144 ))
  OPER("	b	`j0" jumps(L36 ))
  LABEL("L36")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t143 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t142 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t141 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t140 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t139 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t138 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t137 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t136 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t135 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t134 ))
)
ProcFrag(
MipsFrame(
Thread2.run:
Formals(InReg(t54)
	)
Actuals(InReg(t4)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(4)
)
  MOVE("	move	`d0,	`s0" defs(t149 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t150 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t151 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t152 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t153 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t154 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t155 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t156 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t157 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t158 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t54 ) uses(t4 ))
  LABEL("L39")
  MOVE("	move	`d0,	`s0" defs(t55 ) uses(t54 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t55 t0 ) jumps(_BADPTR L11 ))
  LABEL("L11")
  OPER("	addi	`d0,	`s0,	4" defs(t159 ) uses(t55 ))
  MOVE("	move	`d0,	`s0" defs(t146 ) uses(t159 ))
  MOVE("	move	`d0,	`s0" defs(t56 ) uses(t54 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t56 t0 ) jumps(_BADPTR L12 ))
  LABEL("L12")
  OPER("	lw	`d0,	0(`s0)" defs(t160 ) uses(t56 ))
  MOVE("	move	`d0,	`s0" defs(t57 ) uses(t160 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t57 t0 ) jumps(_BADPTR L13 ))
  LABEL("L13")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t57 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t162 ) uses(t57 ))
  OPER("	lw	`d0,	8(`s0)" defs(t161 ) uses(t162 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t161 t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t145 ) uses(t2 ))
  OPER("	sw	`s0,	(`s1)" uses(t145 t146 ))
  MOVE("	move	`d0,	`s0" defs(t58 ) uses(t54 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t58 t0 ) jumps(_BADPTR L14 ))
  LABEL("L14")
  OPER("	addi	`d0,	`s0,	4" defs(t163 ) uses(t58 ))
  MOVE("	move	`d0,	`s0" defs(t148 ) uses(t163 ))
  MOVE("	move	`d0,	`s0" defs(t59 ) uses(t54 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t59 t0 ) jumps(_BADPTR L15 ))
  LABEL("L15")
  OPER("	lw	`d0,	0(`s0)" defs(t164 ) uses(t59 ))
  MOVE("	move	`d0,	`s0" defs(t60 ) uses(t164 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t60 t0 ) jumps(_BADPTR L16 ))
  LABEL("L16")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t60 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t166 ) uses(t60 ))
  OPER("	lw	`d0,	16(`s0)" defs(t165 ) uses(t166 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t165 t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t147 ) uses(t2 ))
  OPER("	sw	`s0,	(`s1)" uses(t147 t148 ))
  OPER("	b	`j0" jumps(L38 ))
  LABEL("L38")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t158 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t157 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t156 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t155 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t154 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t153 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t152 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t151 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t150 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t149 ))
)
ProcFrag(
MipsFrame(
Thread2.setData:
Formals(InReg(t61)
	InReg(t62)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(0)
)
  MOVE("	move	`d0,	`s0" defs(t167 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t168 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t169 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t170 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t171 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t172 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t173 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t174 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t175 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t176 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t61 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t62 ) uses(t5 ))
  LABEL("L41")
  MOVE("	move	`d0,	`s0" defs(t63 ) uses(t61 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t63 t0 ) jumps(_BADPTR L17 ))
  LABEL("L17")
  OPER("	sw	`s0,	0(`s1)" uses(t62 t63 ))
  MOVE("	move	`d0,	`s0" defs(t64 ) uses(t61 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t64 t0 ) jumps(_BADPTR L18 ))
  LABEL("L18")
  OPER("	lw	`d0,	0(`s0)" defs(t177 ) uses(t64 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t177 ))
  OPER("	b	`j0" jumps(L40 ))
  LABEL("L40")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t176 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t175 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t174 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t173 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t172 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t171 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t170 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t169 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t168 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t167 ))
)
ProcFrag(
MipsFrame(
Data.set_field:
Formals(InReg(t65)
	InReg(t66)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(0)
)
  MOVE("	move	`d0,	`s0" defs(t178 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t179 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t180 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t181 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t182 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t183 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t184 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t185 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t186 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t187 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t65 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t66 ) uses(t5 ))
  LABEL("L43")
  MOVE("	move	`d0,	`s0" defs(t67 ) uses(t65 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t67 t0 ) jumps(_BADPTR L19 ))
  LABEL("L19")
  OPER("	sw	`s0,	0(`s1)" uses(t66 t67 ))
  MOVE("	move	`d0,	`s0" defs(t68 ) uses(t65 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t68 t0 ) jumps(_BADPTR L20 ))
  LABEL("L20")
  OPER("	lw	`d0,	0(`s0)" defs(t188 ) uses(t68 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t188 ))
  OPER("	b	`j0" jumps(L42 ))
  LABEL("L42")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t187 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t186 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t185 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t184 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t183 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t182 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t181 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t180 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t179 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t178 ))
)
ProcFrag(
MipsFrame(
Data.increment_one:
Formals(InReg(t69)
	)
Actuals(InReg(t4)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(8)
)
  MOVE("	move	`d0,	`s0" defs(t190 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t191 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t192 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t193 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t194 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t195 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t196 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t197 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t198 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t199 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t69 ) uses(t4 ))
  LABEL("L45")
  MOVE("	move	`d0,	`s0" defs(t71 ) uses(t69 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t71 t0 ) jumps(_BADPTR L21 ))
  LABEL("L21")
  OPER("	lw	`d0,	0(`s0)" defs(t200 ) uses(t71 ))
  MOVE("	move	`d0,	`s0" defs(t70 ) uses(t200 ))
  OPER("	addi	`d0,	`s0,	1" defs(t201 ) uses(t70 ))
  MOVE("	move	`d0,	`s0" defs(t70 ) uses(t201 ))
  MOVE("	move	`d0,	`s0" defs(t72 ) uses(t69 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t72 t0 ) jumps(_BADPTR L22 ))
  LABEL("L22")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t72 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t70 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t203 ) uses(t72 ))
  OPER("	lw	`d0,	12(`s0)" defs(t202 ) uses(t203 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t202 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t189 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t70 ) uses(t189 ))
  MOVE("	move	`d0,	`s0" defs(t73 ) uses(t69 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t73 t0 ) jumps(_BADPTR L23 ))
  LABEL("L23")
  OPER("	lw	`d0,	0(`s0)" defs(t204 ) uses(t73 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t204 ))
  OPER("	b	`j0" jumps(L44 ))
  LABEL("L44")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t199 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t198 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t197 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t196 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t195 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t194 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t193 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t192 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t191 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t190 ))
)
ProcFrag(
MipsFrame(
Data.increment_ten:
Formals(InReg(t74)
	)
Actuals(InReg(t4)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(8)
)
  MOVE("	move	`d0,	`s0" defs(t206 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t207 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t208 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t209 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t210 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t211 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t212 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t213 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t214 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t215 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t74 ) uses(t4 ))
  LABEL("L47")
  MOVE("	move	`d0,	`s0" defs(t76 ) uses(t74 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t76 t0 ) jumps(_BADPTR L24 ))
  LABEL("L24")
  OPER("	lw	`d0,	0(`s0)" defs(t216 ) uses(t76 ))
  MOVE("	move	`d0,	`s0" defs(t75 ) uses(t216 ))
  OPER("	addi	`d0,	`s0,	10" defs(t217 ) uses(t75 ))
  MOVE("	move	`d0,	`s0" defs(t75 ) uses(t217 ))
  MOVE("	move	`d0,	`s0" defs(t77 ) uses(t74 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t77 t0 ) jumps(_BADPTR L25 ))
  LABEL("L25")
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t77 ))
  MOVE("	move	`d0,	`s0" defs(t5 ) uses(t75 ))
  OPER("	lw	`d0,	-4(`s0)" defs(t219 ) uses(t77 ))
  OPER("	lw	`d0,	12(`s0)" defs(t218 ) uses(t219 ))
  OPER("	jalr	`s0" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t218 t4 t5 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t205 ) uses(t2 ))
  MOVE("	move	`d0,	`s0" defs(t75 ) uses(t205 ))
  MOVE("	move	`d0,	`s0" defs(t78 ) uses(t74 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t78 t0 ) jumps(_BADPTR L26 ))
  LABEL("L26")
  OPER("	lw	`d0,	0(`s0)" defs(t220 ) uses(t78 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t220 ))
  OPER("	b	`j0" jumps(L46 ))
  LABEL("L46")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t215 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t214 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t213 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t212 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t211 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t210 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t209 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t208 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t207 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t206 ))
)
ProcFrag(
MipsFrame(
Data.write:
Formals(InReg(t79)
	InReg(t80)
	)
Actuals(InReg(t4)
	InReg(t5)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(0)
)
  MOVE("	move	`d0,	`s0" defs(t221 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t222 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t223 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t224 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t225 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t226 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t227 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t228 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t229 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t230 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t79 ) uses(t4 ))
  MOVE("	move	`d0,	`s0" defs(t80 ) uses(t5 ))
  LABEL("L49")
  OPER("	jal	_yield" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t81 ) uses(t79 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t81 t0 ) jumps(_BADPTR L27 ))
  LABEL("L27")
  OPER("	sw	`s0,	0(`s1)" uses(t80 t81 ))
  MOVE("	move	`d0,	`s0" defs(t82 ) uses(t79 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t82 t0 ) jumps(_BADPTR L28 ))
  LABEL("L28")
  OPER("	lw	`d0,	0(`s0)" defs(t231 ) uses(t82 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t231 ))
  OPER("	b	`j0" jumps(L48 ))
  LABEL("L48")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t230 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t229 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t228 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t227 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t226 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t225 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t224 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t223 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t222 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t221 ))
)
DataFrag(
	.data
L30:	.asciiz	"field = "
)
ProcFrag(
MipsFrame(
Data.field_print:
Formals(InReg(t83)
	)
Actuals(InReg(t4)
	)
BadPtr(_BADPTR)
BadSub(_BADSUB)
maxArgOffset(4)
)
  MOVE("	move	`d0,	`s0" defs(t232 ) uses(t31 ))
  MOVE("	move	`d0,	`s0" defs(t233 ) uses(t16 ))
  MOVE("	move	`d0,	`s0" defs(t234 ) uses(t17 ))
  MOVE("	move	`d0,	`s0" defs(t235 ) uses(t18 ))
  MOVE("	move	`d0,	`s0" defs(t236 ) uses(t19 ))
  MOVE("	move	`d0,	`s0" defs(t237 ) uses(t20 ))
  MOVE("	move	`d0,	`s0" defs(t238 ) uses(t21 ))
  MOVE("	move	`d0,	`s0" defs(t239 ) uses(t22 ))
  MOVE("	move	`d0,	`s0" defs(t240 ) uses(t23 ))
  MOVE("	move	`d0,	`s0" defs(t241 ) uses(t30 ))
  MOVE("	move	`d0,	`s0" defs(t83 ) uses(t4 ))
  LABEL("L51")
  MOVE("	move	`d0,	`s0" defs(t85 ) uses(t83 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t85 t0 ) jumps(_BADPTR L29 ))
  LABEL("L29")
  OPER("	lw	`d0,	0(`s0)" defs(t242 ) uses(t85 ))
  MOVE("	move	`d0,	`s0" defs(t84 ) uses(t242 ))
  OPER("	jal	_yield" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  OPER("	la	`d0,	L30" defs(t243 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t243 ))
  OPER("	jal	_print" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t4 ) uses(t84 ))
  OPER("	jal	_printint" defs(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ) uses(t4 ))
  OPER("	// Call sink" uses(t31 t4 t5 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t24 t25 t2 t3 ))
  MOVE("	move	`d0,	`s0" defs(t86 ) uses(t83 ))
  OPER("	beq	`s0,	`s1,	`j0" uses(t86 t0 ) jumps(_BADPTR L31 ))
  LABEL("L31")
  OPER("	lw	`d0,	0(`s0)" defs(t244 ) uses(t86 ))
  MOVE("	move	`d0,	`s0" defs(t2 ) uses(t244 ))
  OPER("	b	`j0" jumps(L50 ))
  LABEL("L50")
  MOVE("	move	`d0,	`s0" defs(t30 ) uses(t241 ))
  MOVE("	move	`d0,	`s0" defs(t23 ) uses(t240 ))
  MOVE("	move	`d0,	`s0" defs(t22 ) uses(t239 ))
  MOVE("	move	`d0,	`s0" defs(t21 ) uses(t238 ))
  MOVE("	move	`d0,	`s0" defs(t20 ) uses(t237 ))
  MOVE("	move	`d0,	`s0" defs(t19 ) uses(t236 ))
  MOVE("	move	`d0,	`s0" defs(t18 ) uses(t235 ))
  MOVE("	move	`d0,	`s0" defs(t17 ) uses(t234 ))
  MOVE("	move	`d0,	`s0" defs(t16 ) uses(t233 ))
  MOVE("	move	`d0,	`s0" defs(t31 ) uses(t232 ))
)
